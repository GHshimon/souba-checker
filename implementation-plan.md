# 相場チェッカー 実装計画書

> せどり・転売事業用 / 認識精度重視 / 完全無料運用

---

## 🆕 追加仕様メモ（2025-05-29 受領）

追加要望として受領した機能。フェーズ・優先度に反映済み。

| # | 機能 | 概要 | 反映先 |
|---|---|---|---|
| ① | 複数画像で1商品の精度向上 | 最大5枚を同時送信→Geminiが統合判定 | MUST / Phase 2 |
| ② | 1画像に複数商品→自動分類 | 画像内の全商品を検出・分類し各商品ごとに検索 | SHOULD / Phase 3 |
| ③ | ドラッグ&ドロップ対応 | PC操作想定。D&Dエリアでファイル追加 | MUST / Phase 2 |
| ④ | 背景除去処理 | 認識精度向上のため送信前に背景を除去 | SHOULD / Phase 3 |
| ⑤ | 型番違い・類似品リストアップ | 特定後に類似型番を提示し各々の価格検索 | SHOULD / Phase 3 |

---

## 1. 要件サマリー

| 項目 | 内容 |
|---|---|
| **用途** | せどり・転売の事業利用 |
| **コア機能** | 写真 → 商品特定 → 各サイト相場（最安値・平均値） |
| **対象サイト** | 楽天市場、ヤフオク、メルカリ |
| **重視ポイント** | ①認識精度の向上 ②完全無料運用 |
| **デバイス** | スマホ優先（PWA）、PCでも動作 |

---

## 2. アーキテクチャ

```
┌─────────────────────────────────────────┐
│  スマホブラウザ / PWA                    │
│  Vite + React + IndexedDB                │
└────────────────┬────────────────────────┘
                 │ HTTPS
┌────────────────▼────────────────────────┐
│  Cloudflare Pages + Pages Functions     │
│  ┌─────────────────────────────────┐    │
│  │ /api/identify  → Gemini API     │    │
│  │ /api/prices/rakuten → 楽天API    │    │
│  │ /api/prices/yahoo  → Yahoo API   │    │
│  │ /api/cache    → KV参照           │    │
│  └─────────────────────────────────┘    │
│  APIキーは環境変数で秘匿                  │
└─────────────────────────────────────────┘
```

**選定理由：**
- **Cloudflare Pages + Functions**：フロントとバックエンドを1つのデプロイで管理。完全無料枠で十分。
- **Vite + React**：既存スキル（`vite-react-web`）を活用、ビルド高速、PWA化容易
- **IndexedDB**：履歴保存。サーバーレスでランニングコスト0

---

## 3. 無料枠の運用設計

| サービス | 無料枠 | 想定利用 | 余裕度 |
|---|---|---|---|
| **Cloudflare Pages** | 無制限ホスティング | 静的配信 | ◎ |
| **Cloudflare Functions** | 100,000 req/日 | 1商品=3 req想定で33,000商品/日処理可 | ◎ |
| **Cloudflare KV** | 100,000 read/日, 1,000 write/日 | 結果キャッシュ | ◎ |
| **Gemini API (Flash)** | 1,500 req/日 (RPD) | 1商品=1 req | 1日500商品まで |
| **楽天市場 API** | 1 req/秒 | レート制限のみ | ◎ |
| **Yahoo!ショッピングAPI** | 50,000 req/日 | 余裕 | ◎ |

**ボトルネック**：Gemini APIの1,500 req/日 → **1日500商品の認識上限**

→ 対策：KVキャッシュで同一商品の再認識を防ぐ（写真のハッシュ値で照合）

---

## 4. 機能リスト（優先度付き）

### MUST（MVPに必須）
- [x] カメラ撮影 / フォトライブラリ選択
- [ ] **複数画像アップロード**（最大5枚）→ Geminiに同時送信して統合判定 🆕①
- [ ] **ドラッグ&ドロップでファイル追加**（PC操作対応） 🆕③
- [ ] Gemini APIで商品特定（バックエンド経由）
- [ ] 楽天市場 API で実価格取得（最安値・平均値）
- [ ] ヤフオク API で実価格取得（落札相場・出品中相場）
- [ ] メルカリは検索リンク（API無いため）
- [ ] 認識結果のユーザー編集（誤認識時に検索ワードを修正）

### SHOULD（精度・効率向上）
- [ ] **1画像に複数商品 → 自動分類・各商品ごとに価格検索** 🆕②
  - Geminiで画像内の全商品を検出してリスト化
  - 商品カード形式で並べて各々のサイトリンクを生成
- [ ] **型番違い・類似品のリストアップ → 各々の価格検索** 🆕⑤
  - 特定済み型番をもとに「色違い / 容量違い / 世代違い」を提示
  - それぞれに楽天・ヤフオクの価格検索を追加表示
- [ ] **背景除去処理**（送信前に前処理して認識精度向上） 🆕④
  - ブラウザ側で `@imgly/background-removal` を使いオフライン処理
  - 元画像と除去済み画像を両方Geminiに送るオプション
- [ ] 認識結果のキャッシュ（同じ商品を再撮影しても無料）
- [ ] 履歴の検索・絞り込み
- [ ] 利益計算（仕入れ値入力 → 想定利益表示）
- [ ] PWA化（ホーム画面追加、オフライン履歴閲覧）
- [ ] 価格比較ビュー（3サイト横並びテーブル）

### COULD（将来）
- [ ] 価格トレンドグラフ（同商品を複数回チェックして履歴化）
- [ ] CSV エクスポート
- [ ] バーコード読取（JANコードで一発特定）
- [ ] 商品状態（新品/中古ABCランク）の自動判定

### WON'T（やらない）
- ~~メルカリのスクレイピング~~ → 規約違反・破綻リスク高
- ~~クラウド同期~~ → ランニングコスト発生、個人事業なら不要
- ~~ユーザー認証~~ → 単独利用前提

---

## 5. メルカリ対応の決断

メルカリは公式APIが無いため、選択肢は以下：

| 案 | 取得できる | コスト | リスク |
|---|---|---|---|
| **A: 検索リンクのみ**（推奨） | なし（手動確認） | 0円 | なし |
| B: オークファンAPI | 落札済み価格 | 有料プラン要 | コスト発生 |
| C: スクレイピング | 出品中価格 | 0円 | 規約違反、IP遮断 |

**推奨：A案** — 楽天とヤフオクで自動取得した相場で十分な判断材料になります。メルカリは1タップで検索ページに飛べるので、最終確認だけ手動で。

---

## 6. ディレクトリ構成案

```
souba-checker/
├── functions/                    # Cloudflare Functions (API)
│   ├── api/
│   │   ├── identify.ts          # Gemini呼び出し（複数画像対応）
│   │   ├── similar.ts           # 類似型番リストアップ 🆕⑤
│   │   ├── prices/
│   │   │   ├── rakuten.ts       # 楽天API
│   │   │   └── yahoo.ts         # Yahoo API
│   │   └── _middleware.ts       # 共通ミドルウェア（レート制限など）
│   └── _utils/
│       ├── gemini.ts            # Geminiクライアント
│       ├── cache.ts             # KVキャッシュ
│       └── hash.ts              # 画像ハッシュ
├── src/                          # React フロントエンド
│   ├── components/
│   │   ├── Dropzone/            # D&D + クリック + カメラ 🆕③
│   │   ├── ImagePreview/        # 複数画像プレビュー・並び替え 🆕①
│   │   ├── BgRemoval/           # 背景除去ON/OFFと比較表示 🆕④
│   │   ├── MultiProduct/        # 複数商品カードグリッド 🆕②
│   │   ├── SimilarModels/       # 類似型番リスト 🆕⑤
│   │   ├── ResultView/          # 認識結果＋編集
│   │   ├── PriceTable/          # 3サイト価格比較
│   │   └── History/             # 履歴一覧
│   ├── hooks/
│   │   ├── useIdentify.ts
│   │   ├── useSimilar.ts        # 類似型番フェッチ 🆕⑤
│   │   ├── useBgRemoval.ts      # 背景除去処理 🆕④
│   │   ├── usePrices.ts
│   │   └── useHistory.ts        # IndexedDB
│   ├── lib/
│   │   ├── api.ts               # バックエンド呼び出し
│   │   └── db.ts                # IndexedDB (idb)
│   └── App.tsx
├── public/
│   ├── manifest.json            # PWA
│   └── icons/
├── wrangler.toml                # Cloudflare設定
├── vite.config.ts
└── package.json
```

---

## 7. フェーズ別実装計画

### 🚀 Phase 1: バックエンドMVP（推定2〜3日）
**ゴール：実価格が取れるバックエンドを動かす**

1. Cloudflare アカウント＋Pagesプロジェクト作成
2. Gemini API キー取得（Google AI Studio、無料）
3. 楽天 API アプリID取得（Rakuten Developers）
4. Yahoo! Developer Network 登録、Client ID取得
5. `/api/identify` 実装（画像受け取り → Gemini → JSON返却）
6. `/api/prices/rakuten` 実装（楽天市場検索API）
7. `/api/prices/yahoo` 実装（ヤフオク落札相場API）
8. ローカルでcurl叩いて動作確認

### 🎨 Phase 2: フロント刷新（2〜3日）
**ゴール：プロトを置き換える正式版フロント**

1. Vite + React プロジェクト作成
2. 既存プロトのUIを移植
3. **複数画像アップロード UI**（最大5枚、プレビュー付き）🆕①
4. **ドラッグ&ドロップ対応**（PCとスマホ両対応のDropzone） 🆕③
5. 認識結果の編集UI（型番・検索ワードを手動修正可）
6. 3サイト価格比較テーブル
7. IndexedDB で履歴保存
8. デプロイ → スマホ・PC両方で動作確認

### 🔧 Phase 3: 精度向上＆効率化（3〜4日）
**ゴール：実用レベルにする**

1. KVキャッシュ実装（画像ハッシュで判定）
2. Geminiプロンプト調整（複数画像から型番抽出を強化）
3. **背景除去処理**（`@imgly/background-removal` でブラウザ内処理） 🆕④
   - 処理前・処理後を並べてプレビュー表示
   - ON/OFFトグルで比較できるUI
4. **1画像に複数商品 → 自動分類機能** 🆕②
   - Geminiへのプロンプトを「全商品を検出してJSONリスト返却」に切り替え
   - 商品カードを複数並べて各々に価格検索ボタン
5. **型番違い・類似品リストアップ** 🆕⑤
   - 特定後に2回目のGemini呼び出し（類似型番の列挙）
   - 各類似型番に楽天・ヤフオクの相場リンクを自動生成
6. 利益計算機能（仕入れ値入力欄）
7. PWA化（vite-plugin-pwa）
8. オフライン時の履歴閲覧

### 🎯 Phase 4: 任意機能（後日）
- バーコード読取（QuaggaJS or @zxing/library）
- 価格トレンドグラフ
- CSVエクスポート

---

## 8. 精度向上の具体策

事業用なので**誤認識＝損失**。以下の手当てを段階的に：

### 複数画像の統合判定（🆕①）
```
[送信構造]
画像1（全体像）+ 画像2（型番ラベル）+ 画像3（付属品）
     ↓ 全部まとめてGeminiに1回で送信
     ↓ Geminiが全画像を横断して型番を確定
```
- 1枚目：全体像（必須）
- 2枚目：型番・ラベルのアップ（強く推奨）
- 3〜5枚目：パッケージ・シリアル番号・付属品（任意）

### ドラッグ&ドロップ UI（🆕③）
```
┌──────────────────────────────────┐
│  ここにファイルをドロップ          │
│  または クリックして選択           │
│                                  │
│  [img1] [img2] [+追加]           │
└──────────────────────────────────┘
```
- `react-dropzone` を使用（DnD + クリック選択を1コンポーネントで処理）
- スマホのタップ選択とも両立

### 背景除去（🆕④）
- ライブラリ：`@imgly/background-removal`（完全ブラウザ内処理・無料）
- 処理フロー：アップロード → 背景除去 → 元画像と除去済みを両方Geminiに送信
- コスト：APIコール数は変わらず（1回の呼び出しに両方含む）
- 注意：処理に2〜5秒かかるためローディング表示を丁寧に

### 複数商品の自動分類（🆕②）
- Geminiへのプロンプトを「商品が複数あれば全て列挙」モードに切替
- 返却形式：`products: [{ product_name, search_query, ... }, ...]`
- UI：商品カードをグリッドで並べて個別に価格検索可能

### 型番違い・類似品（🆕⑤）
- 商品特定後、Geminiに2回目の呼び出し（軽量モデル可）
  ```
  「BUFFALO LUA4-U3-AGTE」の類似・型番違い製品を列挙してください。
  例：世代違い（LUA3-U2-AGT）、色違い、OEM品など。
  ```
- 類似品リストを折りたたみUIで表示
- 各類似品に楽天・ヤフオク・メルカリの検索リンクを自動生成

### Geminiプロンプト最適化
```
あなたはせどり業者向けの商品特定AIです。
与えられた複数の写真から商品を一意に特定してください。

優先順位：
1. 製品ラベル/シールに記載の型番（最重要）
2. 製品本体の刻印・印字
3. パッケージのバーコード/JANコード
4. 製品の外観・色・形状

不確実な場合は confidence を "low" にし、
何が不足しているか missing_info に記載してください。
```

### 誤認識リカバリ
- 認識結果画面に「型番を修正」「検索ワードを編集」ボタン
- 修正履歴をローカル保存 → 同じ間違いを学習データに活用

---

## 9. APIキー管理

```toml
# wrangler.toml
[vars]
# 公開してOKなもの

[env.production]
# シークレット（wrangler secret put で登録）
# GEMINI_API_KEY
# RAKUTEN_APP_ID
# YAHOO_CLIENT_ID
```

```bash
# シークレット登録コマンド
npx wrangler pages secret put GEMINI_API_KEY
npx wrangler pages secret put RAKUTEN_APP_ID
npx wrangler pages secret put YAHOO_CLIENT_ID
```

---

## 10. 次の一歩

最短ルートで Phase 1 から着手する場合：

1. **Cloudflare アカウント作成**（あれば飛ばす）
2. **Gemini API キー取得** → https://aistudio.google.com/apikey
3. **楽天アプリID取得** → https://webservice.rakuten.co.jp/
4. **Yahoo! Client ID取得** → https://e.developer.yahoo.co.jp/

3つのキーが揃ったら、Phase 1 のコードを書き始められます。

---

## まとめ

- **完全無料で運用可能**（Geminiの1日500商品制限が唯一の上限）
- **メルカリは検索リンクのみ**が現実的（APIなし）
- **精度向上は5段構え**：複数画像統合・D&D・背景除去・複数商品分類・類似型番列挙
- **実装は4フェーズ、コア完成まで7〜10日**（追加仕様分を加算）

### 追加仕様5件の技術的な懸念点メモ

| 仕様 | 懸念 | 対策 |
|---|---|---|
| ①複数画像統合 | Geminiのリクエストサイズ上限 | 1枚あたり512px圧縮で5枚でも十分収まる |
| ②複数商品分類 | 商品が多い場合のUI崩れ | カード数に上限（最大8件）を設けスクロール化 |
| ③D&D | iOS Safariのドロップ非対応 | モバイルはタップ選択にフォールバック |
| ④背景除去 | 処理が重く2〜5秒かかる | 任意機能（ON/OFFトグル）にして通常はスキップ |
| ⑤類似型番 | Gemini追加呼び出しでAPI消費増 | 結果をキャッシュ、同型番の2回目は再利用 |

